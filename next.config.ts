import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains: ['localhost:3000', 'localhost:3001', 'localhost:3002','lh3.googleusercontent.com']
  }
};

export default nextConfig;
